
import { ConfirmForm } from "@/components/confirm-form";
import { Suspense } from "react";

const ConfirmPage = () => {
  return (
    <Suspense>
      <ConfirmForm/>
    </Suspense>
  );
};

export default ConfirmPage;
