
import { ResetForm } from "@/components/auth/reset-form";
import { Suspense } from "react";

const AuthRest = () => {
  return (
    <Suspense>
      <ResetForm />
    </Suspense>
  );
};

export default AuthRest;
